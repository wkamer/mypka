name: CI
# Quality gate for {{PROJECT}}. No auto-deploy — deploys are manual.
on:
  pull_request:
  push:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: lint/format (wire at Architecture gate)"

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: tests + coverage threshold"

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: SAST + SCA/dependency scan + secrets scan + SBOM"

  evals:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: LLM/agent eval suite (remove this job if no AI components)"

  build:
    runs-on: ubuntu-latest
    needs: [lint, test, security]
    steps:
      - uses: actions/checkout@v4
      - run: echo "TODO: reproducible build"
